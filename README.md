# Graph-Neural-Network-for-Fraud-Detection
Implement a graph neural network to detect fraudulent transactions by modeling users and transactions as a graph. Uses PyTorch Geometric with GCN architecture for fraud detection.
